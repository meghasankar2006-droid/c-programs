#include <stdio.h>

int main() {
    char w;
    Scanf("%c",&w);
    if(w==a || w==e || w==i ||w==o || w==u || w==A || w==E || w==I || w==O || w==U ){
        printf("Vowels");
    }
    else{
        printf("Consonants");
    }

    return 0;
}